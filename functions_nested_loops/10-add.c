#include "main.h"

/**
 * add - adds two integers and returns the result
 * @t: integer added to o
 * @o: integer added to t
 * Return: sum of t and o
 */
int add(int t, int o)
{
	int x;

	x = t + o;
	return (x);
}
