#include "main.h"
/**
 * print_rev - print the string in reverse
 * @s: printed string
*/
void print_rev(char *s)
{
	int A;
	int B;

	for (s = A; A != '\0'; A++)
	{
	}
	for (B = A; B > 0; B--)
	{
		_putchar(*s);
		A--;
	}
	_putchar('\n');
}
