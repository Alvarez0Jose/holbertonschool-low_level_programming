#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	int intType;
	float floatType;
	double doubleTyper;
	char charTyper;

	printf("Size of int: %zu bytes\n", sizeof(intType));

	return (0);

