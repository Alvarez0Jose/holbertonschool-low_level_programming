#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
*/

int main(void)
{
	/*print all lowercase letters*/
	putchar (lower_x);
	putchar ('\n');
	getch();

	for (int = x 'a'; x <= 'z'; x++)
}
