#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
*/

int main(void)
	/*print all lowercase letters*/
	putchar (ch);
	putchar ('\n');
	getch();

	for (ch = 'a'; ch <= 'z'; ch++)
